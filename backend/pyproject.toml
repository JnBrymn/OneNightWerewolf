[project]
name = "ping-backend"
version = "0.1.0"
description = "FastAPI backend for Ping application"
requires-python = ">=3.8"
dependencies = [
    "fastapi==0.104.1",
    "uvicorn[standard]==0.24.0",
]

